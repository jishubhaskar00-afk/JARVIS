# Copy this file to ".env" and fill in your real keys.
# ".env" itself must NEVER be committed to git (it's already in .gitignore).

# Groq key (starts with gsk_) or OpenAI key (starts with sk-)
AI_API_KEY=

# https://newsapi.org key
NEWS_API_KEY=
